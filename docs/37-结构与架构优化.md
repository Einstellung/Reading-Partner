# 结构与架构优化

对 `main` @ 2499820 的代码库做了一轮架构审计，22 项。这份文档既是做过什么的记录，也是剩下那部分的执行计划。

A 里除 A7 外全部落地，B 全部落地。已合进 `origin/main` @ 03981e6；B1、B4、B8、B13 做完在 `arch/b-appchain`，还没合。下面的行数和文件数按合完那条分支的树算。

`arch/b-appchain` 从 ca682f3 分出，不含 B3、B5、B6、B7、B10、B12，合的时候 `reading/engine/gesture/`、`budget/ladder.ts`、`info/briefing/presence.ts`、`observation/sweeps.ts`、`reading/engine/raster.ts` 这几处移动要手工处理。

顺带产出：坑 119（`mock.module`）、8a5ced7（WebKit 拿到一次触摸会做什么，改成实测）、69a6deb（iPad 模拟器上的真触摸测试）、81ffc38（dev 期的 sim bridge 收到回环）。

---

## A

### A1 让 `tests/layering.test.ts` 下探到真实目录粒度

完成：ddfda52（按最长 LAYER key 前缀解析 + 任何含源文件的目录未登记即失败）、5dd3180。

5dd3180 是复核出来的：`KNOWN_CYCLES` 原来用并查集把两端合并，八对塌成两个团，团内三个节点以上的环整个不可见；改成只丢那两条有向边、不动节点。同一个 commit 把 CLAUDE.md 的登记规则改成"任何深度的目录都要登记"。

### A2 `settings.json` 的两个真相与退出丢写

完成：e576ae8，加复核出的 ae8056f（面板开着时推迟的那次回读，必须等它本该读过的那次保存落盘）和 49ec00c。

### A3 外壳 bootstrap 收口，六个错误槽全注册

完成：319a4ac，加 a0500fa（测试里的假 window 会漏进别的文件）。`installFetchBridge()` 是从 App 删掉，不是复制给 PhoneApp——`main.tsx` 已经无条件调过。

### A4 `InfoCall` 的拒答走成了错误，`rehydrateMessage` 没被复用

完成：b05a6d0。拒答这条线复核后又改了三次：324ff3e（拒答是 app 在说话，重发时不当模型的话回放）、23eb437 与 eef8b68（拒答在源头清掉失败标记）、366cf44。

### A5 三个自绘浮层吃不到安全区

完成：191b523，加 d58aaf6（安全区要在第一帧画出来之前读到）和 7a28441。`placePanel` 和 `fitPanelWidth` 都收逐边 margin。笔的调色板那处 `cn()` 改写会重排 class，`shadow-xl` 必须排在 `CARD` 后面。

### A6 下拉行的几何收进原语

完成：60a3983、b0bf061。`primitive-contract.test.ts` 除了 44px 断言，还查调用点不许再声明一遍最小高度。

### A7 等于既有 token 的 hex 字面量

未做。八个值仍在：`#1b1b1b`×23 = `--foreground`，`#555`×12 = `--muted-foreground`，`#dcdcdc`×4 = `--border`（含 `shelf/cardStyles.ts`），`#b91c1c`×5、`#4a3a9e`×5、`#c9c2e8`×4、`#efecfb`×2、`#f0f0f0`×2，src 下共 57 处。

动作：只替换这八个（`text-[#1b1b1b]` → `text-foreground` 等），构造上零视觉变化。剩下的未声明色阶留到 C。

验证：`bun test`；`bunx tsc --noEmit`；改完 `rg -o '\[#(1b1b1b|555|dcdcdc|b91c1c|4a3a9e|c9c2e8|efecfb|f0f0f0)\]' src` 应为空。之后补那条禁止写出与已声明 token 同值的 `[#...]` 的断言。

### A8 三处领域操作从 `.tsx` 收回领域

完成：366d5f4、5c0d6cb。`openDeckFile` 最后叫 `revealDeckFile`（`live.ts` 已有 `openDeck`）；`startTalk` 和 `StartTalkInput` 从 talks 桶里摘掉，只留 `createTalk`，免得有人绕开 `talk-start` 事件。

### A9 四组纯工具去重

完成：7b91a59、e84d4e4。`content-hash.ts` 那条风险不存在——它是零 import 的叶子。计划外多出 `platform/app/host.ts`（`isTauri` 加两个领域各自声明过的 `FetchFn`）；`merge/text.ts` 那个同名无关的 `hashBytes` 改叫 `textDigest`。

### A10 `atomic-fs` 补上读侧

完成：77684c5、5d9aae3。只删了 briefing 的两份私有 `readJson` 和五个本地 `APPDATA` 常量，38 处调用点没动（见 C）。

---

## B

### B1 拆掉 `ui/components` 的 8 个环

完成：`arch/b-appchain` 的 0a73a3e（`base/`）、b48d813（`markdown/`）、c898b36（拆 `common/types.ts`）、daf26a9（`shelf/`）、c85be28（`cardRegistry` 上提 + context）、3dbbe69（`pullToAsk` 换成 `wrapScreen`）。

计划外：`topic-nav.ts` 和 `cover-source.ts` 也跟着搬（前者进 `base/`）；`ChatImage` 就是放宽了 mediaType 的 `CompressedImage`，并掉后消失，`common/types.ts` 里没人用的 `Thread` 一起删；`toolTrace.ts` 跟 `ToolStatus` 一起去了 `src/ai/tool-status.ts`。最后一步把 `KNOWN_CYCLES` 和搭在它上面的成对测试一起删了，无环测试现在走全图。

### B2 拆 `EmbedPdfView.tsx`

完成：c9e10b3（四个类型先走）→ e38aac9（`attach-touch.ts`）→ b1e8d96（`wire-engine.ts`）。`EmbedPdfView.tsx` 落到 443 行，比计划的 650 少。

测试是先写的，复核又补了五轮：52c53ea、35bc5e9、5a666cd、607d8db、043d231。坑 32/118 的引用改到新位置：0b50503、5f35f05、681c1e7。

### B3 `reading/engine/gesture/` 子域

完成：314b807、27846f4。`PagedGestureCtx` 和 `EmbedTool` 跟着进 `gesture/context.ts`——留在 `types.ts` 会让 engine 和 engine/gesture 互相 import；`types.ts` 转出 `EmbedTool`，适配层对外的面没变。

### B4 平台持久化收口

完成：`arch/b-appchain` 的 1aca2ec（`debounced-writer.ts`，退出 flush 第二次是 no-op，因为 `observeAppExit` 不去重）、78f8aca（`store-errors.ts`）、baf7af2（`pull-routes.ts`）。

`pull-coverage.test.ts` 查出手机上从来没注册过按书的缓存失效，pull 之后 info 线程一直被旧缓存挡着；路由表让两个外壳都拿到。`annotations.ts` 的 io 改成参数传入，那份测试从睡三次防抖变成假时钟。

### B5 预算阶梯泛化，两个 turn 去重

完成：f2a2be8（先钉住文案和横档顺序）、73422be。`piContext` 不是收成一份而是整个删掉，`fitToBudget` 自己拼。每根横档怎么计价成了横档上的数据，两份手写计价块才合得成一份。

### B6 章节模型统一，修 deck 规划的空章节

完成：c1af953。

### B7 把多设备协调规则从 `live.ts` 里拿出来

完成：b6cfedc（`info/briefing/presence.ts`）、7803bc1（`observation/sweeps.ts`）。三个 distill 入口现在共用 `gate.run(subject, pass)`，不再各写一遍 add/try/finally-delete。

### B8 `InfoCall` / `InfoHome` 的控制器下沉

完成：`arch/b-appchain` 的 3d4fd2a（先把能判定的那半放进 `info/companion/{anchors,card-actions,call}.ts` 和 `info/sources/session-flow.ts`）、c6f7853（两个 hook）、8fda127。两个组件落到 125 行和 239 行。`keepBriefingArticle` 留在 ui，它把 info 的形状映到 reading 的形状，只有 ui 能同时碰两边。

### B9 两个手机手势 hook 的 DOM 层合一

完成：6a1037d。坑 38 和坑 70 的两条规则写在 `gesture-dom.ts` 头部一次；认领距离留在各自机器的 `shouldClaimTouch` 里。

### B10 `covers.ts` 不再直接开 PDFium

完成：a19476a。判别联合比计划多一支 `no-engine`：引擎不在时不写失败标记，免得一次瞬时故障让一本书一天没有封面。缩放系数改成页宽的函数传入。`tests/layering.test.ts` 加了 `@embedpdf` 只能被 `reading/engine` 和 `smoke` import 的断言。

### B11 `MicButton` 的按压状态机

完成：71c72a0。`arming` 和 `aborting` 把"已要求录音"和"还没起来就松手了"分开。23 条测试，四条竞态规则每条都有一条抽掉规则就红的。

### B12 info 侧的 HTTP 补 Retry-After

完成：e7b9cac。实际比计划描述的坏：429 根本没重试，落到 throw 又被网络错误那个 catch 接住无脑重发；5xx 立刻重发不等。现在两者共用一条先等再试的分支，`Retry-After` 的秒数和 HTTP-date 两种写法都认，上限 30s，没有头就从 500ms 指数退避，等待被这次 run 的 abort signal 打断。没有新增 `src/net`，单源节流仍是描述符的 `pollMinutes`，并发仍是 `sources/pool.ts`。

### B13 `App.tsx` 的阅读会话下沉

完成：`arch/b-appchain` 的 741471a（`CallState` 独立成类型）、8653131（开书/关书成三段序列）、6a7bb79（reducer）、3eafd8f（`reading/session/use-call.ts`），另加 c03c91d 把 turn 收尾的两条规则从 `ui/components/chat` 搬去 `ai/turn-rows.ts`——领域不能 import ui。`App.tsx` 2003 → 1377 行。

`chat-pip` 两处守卫的分歧按规则处理，不当 bug：合成一个 `reading-uncovered` action，守在 `chat-main`。没守卫的那个调用点是阅读角标卡，只在 `chat-main` 里画，可达行为不变。

---

## C 盯着，还不到时候

- 把三份流式对话驱动抽成 `ai/turn-stream.ts`。B8 和 B13 之后三份都从 `.tsx` 挪进了 `.ts`：`reading/session/use-call.ts`、`ui/components/talk/useTalk.ts`、`ui/components/info/use-info-call.ts`；共享的部分又多了 `ai/turn-rows.ts` 和 `ai/tool-status.ts`，剩下的失败分类、拒答处理、partial 保留仍是三套不同的设计。触发线照旧成立：出现第四个流式对话宿主，或者三者之间再出一个像 A4 那样的行为分叉 bug。
- A10 的 `readJson` 现在只有 7 个调用点，`baseDir: BaseDirectory.AppData` 还有 101 处字面量，24 个文件仍在手写 `exists → readTextFile → JSON.parse → 兜底`。触发线不变：读失败策略真的要改（隔离、告知用户、加 schema 版本闸）的那天一次性改完。
- 未声明的色阶 token 化：`#777`×31、`#999`×26、`#eee`×16、`#e6e6e6`×6、`#ececec`×4，`#333`/`#bbb`/`#fafafa` 各 7，`#666`/`#888`/`#d0d0d0`/`#c0392b`/`#8a7fd0` 各 6，`#b45309`×5、`#5fb236`×5。`src/ui/components` 下共 243 处、56 个不同的值，其中 8 个值（55 处）归 A7。A7 没做，所以这条的规模没变。触发线不变：做深色模式或换配色。
- `loadPdfjs`（`fulltext/extract.ts`）提成独立 capability。未变。触发线：pdf.js 大版本升级，或出现第三个消费者。
- `tests/` 不再镜像 `src/`：20 个文件躺在 `tests/` 根，`tests/info` 33 个平铺，`tests/ui/components` 33 个平铺。40 的触发线还没到，两个大目录各差 7 个。这一轮加的测试就吃掉了六个名额，下一轮结构活很可能撞线。
- `ui/components` 的文件数：B1 之后 `library` 4 加 `library/topic` 5，新出的 `shelf` 9、`base` 5、`markdown` 5 都远低于线；`reader` 13、`settings` 12 没动过，仍是离 15 最近的两个；`chat` 11、`info` 11（B8 加了两个 hook）、`common` 10、`phone` 9。`ui` 16 按 D 豁免。触发线不变，只是这次要盯的是 `reader` 和 `settings`。
- `App.tsx` 过 2500 行那条线随 B13 作废：那条工作已经做了，文件现在 1377 行，只剩渲染树、引擎回调和粘贴监听。不再设行数线。

---

## D 明确不做

- `ui/components/ui`（16 个文件）按规则 1 拆分。路径由 `components.json` 的 `ui` 别名钉死，`bunx shadcn add` 按它写文件。同理 `ui/components/lib` 只有一个 9 行的 `cn`，也由别名钉死。
- 把 `SourcesPage.tsx` 的裸 `<input>`、`AnnotationPopup.tsx` 的裸 `<textarea>` 换成 `Input`/`Textarea` 原语。项目自己的文档解释过这两处为什么是裸的。A6 只收下拉行那部分。
- 把 `reading/sources/article.ts` 的 `extractArticle` 换成 info 侧的 `extractReadable`。两者契约不同：前者返回纯文本 + 标题 + `ARTICLE_MAX_CHARS` 截断，喂 Fulltext 页和摘要 prompt；后者返回 `{title, contentHtml, textContent}`、不截断、且在没有 DOMParser 的环境返回 null。A9 只合并了 `decodeEntities` 和 `htmlToText` 的纯文本尾巴（后者以 `stripTagsToText` 留在 sanitize 那侧）。
- 拆 `info/briefing/pipeline.ts`。一个类一件事，六个阶段已经分别委托出去，剩下的是编排加约 150 行的 observable/checkpoint 脚手架。最近的接缝会把一个状态机劈成两半。
- 把 `reading/figures → fulltext` 当成分层违规修掉。domain → capability 是 `MAY_IMPORT` 明确允许的方向。
- 把 `InfoHome` 的 `renderLaunch`/`onOverlayChange`/`pipCards` 当成规则 4 违规。规则 4 管的是叶子组件不按形态分叉，render prop 洞正是 docs/22 定下的机制；B1.6 把 `pullToAsk` 那个布尔也改成了同一种洞。
- 把 `budget/ladder.ts` 当成"领域编排塞进 capability"来判。B5 做它的理由是两份 turn 的重复，不是分层。
- 把 `reading/saved-articles.ts` 搬去 `info/` 当成规则 2 违规。domain → domain 合法且无环。它仍是全项目唯一一条 `reading → info` 的边，顺手清掉更干净，不清也不算债。
- `platform/sync ⇄ platform/sync/merge` 不是环，只有单向边。

---

## 规则要改的地方

- `tests/layering.test.ts` 头部的解析声明和 CLAUDE.md 的登记规则：已改（ddfda52、5dd3180），现在是"按最长 LAYER key 前缀解析，任何深度的目录都要登记"。
- 规则 3 的理由：已改（a2cea29），现在写的是可测试性。
- 规则 1 的"约 15 个文件"还没加那条例外：由生成器或别名钉死路径的目录（`ui/components/ui`、`ui/components/lib`）不适用。
- 规则 1 还是没写明它只管 `src/`。`tests/` 的三个大目录见 C。

## 契约测试

已加：`tests/layering.test.ts` 的深度解析与强制登记、`budget` 不 import `ai`、`@embedpdf` 只能被 `reading/engine` 和 `smoke` import；`primitive-contract.test.ts` 的菜单行 44px 与调用点不得重复声明；`arch/b-appchain` 上的 `platform/sync/pull-coverage.test.ts`、`platform/store-errors.test.ts`、`platform/debounced-writer.test.ts`。`KNOWN_CYCLES` 及其成对测试在 B1 收尾时删除。

还没加：`src/ui/components/` 下不出现与已声明 token 同值的 `[#...]`，等 A7。

---

## 已核实健康

审计时（2499820）机械重建导入图得出的结论，A1 之后大部分由 `tests/layering.test.ts` 自动保证，不要重审。

- capability 纯净性：`ai`、`ai/subagent`、`ai/voice`、`budget`、`fulltext` 里没有任何文件 import 领域、`ui`、`App.tsx` 或 `PhoneApp.tsx`。`budget` 在 src 下什么都不 import。
- `platform/app` 零条出边。
- 除 `main.tsx` 外没有任何文件 import `App.tsx`/`PhoneApp.tsx`。
- 领域和子领域图在真实目录粒度下无环；`platform/sync → platform/sync/merge` 单向。
- `ui/components` 直接 import 领域，是 MAY_IMPORT 允许的方向。

---

## 这一轮的做法教训

一个 worktree 只能有一个写者。并行 agent 共用一个 worktree 会互相盖掉未提交的改动，而且没有任何报错；开工前 `git status --short` 不干净就停。

新加的测试必须靠改坏它保护的那行来验：把断言指着的代码改掉，测试要红，红了再改回来。六轮复核每一轮都查出改不红的测试。

反复出现的两种死断言。一种是假件同步 resolve，而真实实现是异步的：测试里 `await` 之后状态已经到位，真机上还没到，A2 的推迟回读就是这么漏过去的（ae8056f、49ec00c）。另一种是夹具本身就满足断言，被测行为怎么变都过：B1.5 的卡片注册表测试用静态 import 写，有没有 provider 两种情况都通过，改成从 context 取才真的在测东西（c85be28）。
