# 结构与架构优化

对 `main` @ 2499820 的代码库做了一轮架构审计，覆盖七个维度；52 条经过对抗性复核的发现去重后剩 22 项。A 和 B 是要做的，C 和 D 是已经定下的判断。排序按杠杆：先把守门的测试补上，再拆那些挡住别的活的结，最后是纯收益的整理。

审计之后 origin/main 走到了 1843516，两个 commit 都落在 `src/info/briefing/`，那个目录里的行号会有偏移，其余不受影响。

---

## A 现在就做

每项一两个文件，半天以内，互不依赖（除 A2 与 A3 都动两个外壳，串行）。

### A1 让 `tests/layering.test.ts` 下探到真实目录粒度

现状：`entryOf`(:142) 和 `resolveEntry`(:170) 都在第二段截断，`declaredEntries`(:218) 只向下一层。结果 `ui/components/*` 的 39 条边全部塌成自环被丢掉，8 个目录环在测试全绿的情况下合进来了，其中 `library ⇄ library/topic` 正是 CLAUDE.md 点名禁止的形状。

动作：`entryOf`/`resolveEntry` 改成"匹配最长的 LAYER key 前缀"；`declaredEntries` 递归进每个含源文件的目录，未登记即失败。补登记 `ui/components/{chat,common,info,library,library/topic,lib,phone,reader,settings,talk,ui}`、`platform/sync/merge`。此时 8 个环会红，加一张显式 `KNOWN_CYCLES` 白名单把它们列出来，B1 每修一个删一行。

性质：只改测试文件。规模：~40 行。
风险：无生产代码变更。
验证：`bun test tests/layering.test.ts` 应为 6 通过 + 白名单剩 8 条；手工插一条 `chat -> library` 的假 import 应立刻红。

### A2 `settings.json` 的两个真相与退出丢写

三个洞在同一条路径上，一次补完：

1. `settings.json` 在同步范围内（`syncFs.ts:58`）且走字段级合并（`merge/contract.ts:80`），但两个外壳各持一份 `useState<Settings>`（`App.tsx:298`、`PhoneApp.tsx`），`onSyncPulled` 的匹配表（`App.tsx:566-578`、`PhoneApp.tsx:168-170`）都不含它。对端改的字段合并进磁盘后，本机下一次 `applySettings`（`App.tsx:469-472` → `saveSettings` 整对象序列化 `settings.ts:154-165`）把它整个盖回去并推上去。动作：两个外壳的 pull 处理里加 `settings.json` → `loadSettings()` 回填。
2. `settings.ts` 有 500ms 防抖、没有任何退出 flush（全文无 `pagehide`）。退出前 500ms 内的设置改动丢失。动作：照 `annotations.ts:52-79` 补一个 flush，接 `lifecycle.ts:82 observeAppExit`。
3. `platform/sync/state.ts:6` 的注释已经说明自动同步开关为什么不在 `settings.json` 里——保持原样。

性质：逻辑改动。规模：~30 行，两个外壳 + 一个 store。
风险：pull 回填会覆盖用户在 pull 那一瞬正在编辑的设置面板值；把回填限制在 `showSettings === false` 时。
验证：新写 `tests/platform/settings-flush.test.ts`（假时钟 + 注入的 exit target），断言防抖合并、退出 flush、`blockWrites` 时不写。跨设备那一半手工验：iPad 改 `aiLanguage`，桌面拉取后改别的开关，再看 `settings.json`。

### A3 外壳 bootstrap 收口，六个错误槽全注册

`App.tsx:353-389` 与 `PhoneApp.tsx:128-159` 逐字相同的有：corrupt-file 两分支 toast、`loadSettings` → `enforceKnownModel` → `saveSettings` + 提示、`onSettingsSaveError`、`initDeviceSettings().then(setDevice)`、provider 刷新 effect；`configured` 谓词（`App.tsx:1741-1745` / `PhoneApp.tsx:198-202`）一字不差。同步健康 toast 两边一个用 `useRef` 一个用 `useState`，已经分叉。

六个单槽错误处理器：`onCorruptFile`、`onSettingsSaveError`、`onSaveError`、`onThreadSaveError`、`onFulltextError`、`onFiguresError`。App 注册五个，PhoneApp 只注册两个，`onFiguresError` 全项目无人注册。手机上 `InfoCall` 有八处 `appendMessage` 写线程，而 `onThreadSaveError` 在那个外壳上没接——与 `threads.ts:5` 声称的"失败会被暴露（坑 09）"矛盾。

动作：新增 `src/ui/components/common/useShellBootstrap.ts`（`.ts`，照 `useSyncHealth.ts` 的先例），入参 `{ shell, settingsOpen, pushToast }`，出参 `{ settings, setSettings, device, setDevice, providersInfo, configured, syncReport }`，内含上述五块 + 六个注册 + `useRef` 版的 toast-once。`installFetchBridge()` 不进去，`main.tsx:43` 已经无条件调过。

性质：移动 + 少量逻辑统一。规模：~90 行进 hook，两个外壳各减 ~40 行。
风险：effect 依赖数组两边本来不同（PhoneApp 有 `refreshSavedArticles`），别一起塞进 hook；那部分留在各自外壳。
验证：新写 `tests/ui/components/shell-bootstrap.test.ts`，断言 `enforceKnownModel` 的纠正只写回一次、`configured` 在 provider id 不在列表里时为 false、健康 toast 每次 alert 转换只响一次。

### A4 `InfoCall` 的拒答走成了错误，`rehydrateMessage` 没被复用

`ai/agent.ts:212`：`const refuse = params.onRefusal ?? ((message) => onError(message))`。`InfoCall.tsx:413-478` 的 `runAgent` 完全不传 `onRefusal`，所以 info 侧的模型拒答显示成红色报错。`App.tsx:945` 和 `useTalk.ts:334` 都传了。

同时 `chatParts.ts:237` 已导出 `rehydrateMessage`，`App.tsx:167-174` 和 `useTalk.ts:54-61` 各自把那四行重新摊了一遍。

性质：逻辑修正 + 删重复。规模：~15 行。
验证：`tests/ui/components/` 下加一条，用假的 `runAgentTurn` 触发 refusal，断言消息落在 notice 而非 error。

### A5 三个自绘浮层吃不到安全区

`AnnotationPopup.tsx:24`、`PenToolbar.tsx:38`、`CallBubble.tsx:35` 各自 `const MARGIN = 8` 传给 `placePanel`（`common/panel-position.ts:57-85`），只按视觉视口夹逼，从不看 inset。`ui/overlay.tsx:110` 的 `useOverlaySafePadding()` 返回逐边 `max(inset, 8)`，所有 Radix 浮层都用了，只有这三个没有——正是坑 74 的场景。它们不注册 `<OverlayLayer />` 是对的（`overlay-layer.ts` 头部点名了这三个）。

动作：`placePanel` 的 `margin` 放宽成 `number | SafeAreaInsets`（类型已在 `common/safe-area.ts`），逐边夹；三处改传 `useOverlaySafePadding()`。顺手把 `AnnotationPopup.tsx:97`、`PenToolbar.tsx:211`、`CallBubble.tsx:99` 的 `OVERLAY_Z` 模板串拼接改成 `cn()`。

性质：逻辑改动，桌面无 inset 时行为不变（`max(inset,8)` 退化成 8）。规模：~25 行。
验证：`tests/ui/components/panel-position.test.ts` 加逐边 inset 用例。

### A6 下拉行的几何收进原语

`ui/dropdown-menu.tsx:105` 的 `ITEM_BASE` 还是 shadcn 出厂的 `px-2 py-1.5 text-sm`，三个调用点各自补齐 36/44px：`CardMenu.tsx:31-32` 与 `OutlinePane.tsx:21-22` 逐字相同，`MoreMenu.tsx:46-49` 是同一串加图标规则。规则 5 要求触摸目标写在原语的变体表里。

动作：把行几何做成 `DropdownMenuItem` 的默认（或 `size="row"` 变体），删三处本地常量。
性质：移动。规模：~20 行。
验证：`tests/ui/components/primitive-contract.test.ts` 补一条菜单行 44px 断言。

（同一发现里"把 `SourcesPage` 的裸 input、`AnnotationPopup` 的裸 textarea 换成原语"那半条不做，见 D。）

### A7 等于既有 token 的 hex 字面量

`src/ui/components` 下 248 处 `[#...]`。其中八个值与 `styles.css:44-82` 已声明的 token 完全相同：`#1b1b1b`×23 = `--foreground`，`#555`×12 = `--muted-foreground`，`#dcdcdc`×4 = `--border`（含 `settings/cardStyles.ts:5`，九张设置卡在用），`#b91c1c`×5、`#4a3a9e`×5、`#c9c2e8`×4、`#efecfb`×2、`#f0f0f0`×2。

动作：只替换这八个（`text-[#1b1b1b]` → `text-foreground` 等），构造上零视觉变化。剩下的未声明色阶（`#777`×31、`#999`×26、`#eee`×16 等）留到 C。
性质：sed。规模：57 处。
验证：`bun test`；`bunx tsc --noEmit`；改完 `rg -o '\[#(1b1b1b|555|dcdcdc|b91c1c|4a3a9e|c9c2e8|efecfb|f0f0f0)\]' src` 应为空。

### A8 三处领域操作从 `.tsx` 收回领域

- 开 deck：`openPath(await join(await appDataDir(), file))` 同一 try/catch 出现在 `TalksSection.tsx:86-92` 和 `DeckDialog.tsx:317-324`，是仅有的两个 import `@tauri-apps/api/path` 的 `.tsx`。→ `src/reading/slides/` 加 `openDeckFile(file)`。
- 开讲：`startTalk(...)` + `logEvent(topicId, "talk-start", {...})` 出现在 `LibraryScreen.tsx:114-118` 和 `TalksSection.tsx:100-104`。→ `src/reading/talks/` 加 `createTalk(topicId, materials)`。
- `candidatesFor(topic)`（`TalksSection.tsx:35-44`）在 `.tsx` 里拼 `MaterialCandidate[]`，那个类型归 `reading/talks`。→ 移成 `talkCandidates(topic)`。

性质：移动 + 改调用点。规模：~60 行。
验证：新写 `tests/reading/talks/candidates.test.ts`：无 hash 的文件被跳过，`loadAnnotations` 拒绝时标记数记 0，`createTalk` 只记一条 `talk-start` 且带材料数。

### A9 四组纯工具去重

- `contentHash`（`platform/app/library.ts:24-28`）与 `hashBytes`（`platform/sync/content.ts:16-20`）是同一配方，注释自陈是故意的副本（library.ts 在模块作用域 import Tauri fs 插件，sync 的测试不能碰）。这是该拆文件的理由，不是该复制函数的理由。动作：新建无 fs 依赖的叶子 `src/platform/app/content-hash.ts`，两边 import；`sync/merge/text.ts:48` 那个同名但无关的同步 FNV 改名 `textDigest`。两个 hash 的输出从不比较：sync 的 `hashBytes` 只在 `engine.ts:262/284/354/426` 判字节变没变，library 的 `contentHash` 出书 id，`sync/books.ts:27-29` 从 `library.json` 的 key 读书 id 而不重算。改 sync 那个摘要只会让快照失效重同步一次。所以这是普通去重。
- `isTauri()` 同体五份（`clipboard.ts:6` 已导出、`image-proxy.ts:19`、`ai/fetch-bridge.ts:25`、`reading/papers/http.ts:14`、`info/extract/http.ts:13`）。统一从 `platform/app` 导出一份。
- `decodeEntities` 逐字相同两份（`reading/sources/article.ts:25-35`、`info/extract/sanitize.ts:166-176`），`htmlToText` 尾部管线逐字相同。动作：只把这两个纯函数留一份（放 `info/extract/sanitize.ts`，`reading/sources/article.ts` import 它），不动两侧的抽正文实现（见 D）。
- `{ now, sleep, setTimer }` 三件套逐字出现在 `prep/live.ts:259-266`、`notes/live.ts:137-143`、`slides/live.ts:369-375`、`briefing/live.ts:486-490`，而 `ai/observable-run.ts:36-40` 已经声明了 `RunTimers`。动作：那里导出 `realTimers`，四处展开。

性质：三条纯移动，`content-hash.ts` 是新叶子文件。规模：~80 行。
风险：`content-hash.ts` 若不小心 import 了任何带 fs 的东西，同步测试会连锁失败。
验证：`bun test tests/platform/sync/`。

### A10 `atomic-fs` 补上读侧

写侧有 `writeTextAtomic`/`readGuardedJson`，读侧没有：`exists → readTextFile → JSON.parse → 兜底` 这一块在 25 个文件里出现 38 次，`baseDir: BaseDirectory.AppData` 字面量 113 处，五个文件各自定义了本地 `APPDATA` 常量，`info/briefing/pool-store.ts:37` 与 `publish.ts:116` 有两份逐字相同的私有 `readJson<T>`。

动作：只做加法——`src/platform/app/atomic-fs.ts` 导出 `APPDATA`、`readJson<T>(file, validate?)`（缺失 → null，解析/IO 失败 → warn + null）、`readJsonOr<T>(file, fallback, validate?)`；删掉 briefing 那两份私有副本和五个本地常量。38 处调用点不动，往后新写的 store 一律用它，旧的顺手改（见 C）。
性质：新增 + 删两份副本。规模：~50 行。
验证：`bun test tests/platform/`。

---

## B 接着做

### B1 拆掉 `ui/components` 的 8 个环

前置 A1。六个子动作，`common/` 那三个必须串行（同一目录），其余并行。

1. `base/`：`git mv src/ui/components/common/{buttons.ts,icons.tsx,overlay-layer.ts,safe-area.ts} src/ui/components/base/`。这四个文件不 import 任何东西（已核）。改 27 个引用点和 `ui/{button,checkbox,switch,select,overlay}.tsx` 里的 `@/ui/components/common/...` → `@/ui/components/base/...`，改 `styles.css:176` 的注释。杀 `common ⇄ ui`。纯移动。
2. `markdown/`：`git mv common/{Markdown.tsx,MarkdownRenderer.tsx,proseCss.ts,Markdown.stream.test.tsx} reader/FigureCard.tsx src/ui/components/markdown/`。`MarkdownRenderer.tsx:25` 是 `FigureCard` 唯一的非测试消费者。杀 `common ⇄ reader`。纯移动（`FigureCard` 的 `../common/Markdown` 变 `./Markdown`）。
3. 拆 `common/types.ts`（87 行）：`ToolStatus` 连同 `common/toolTrace.ts` 的 `appendRunningTool`/`resolveToolStatus` 移到 `src/ai/tool-status.ts`（它们描述的是 agent 的工具调用，不是像素；`ai` 是 capability，ui/reading/info 都可以 import，`ai` 不 import 任何领域，合法）；阅读批注那半移到 `reader/types.ts`；剩下的聊天行类型移到 `chat/types.ts`。`common/types.ts` 消失。杀 `chat ⇄ common`。不是纯移动：`ChatImage` 顺手并到 `ai/image-utils.ts` 的 `CompressedImage`。
4. `shelf/`：`git mv library/{CardMenu.tsx,CoverBand.tsx,cardStyles.ts,file-title.ts,BookCard.tsx,TopicCard.tsx,topic-shelf.ts} src/ui/components/shelf/`；`git mv library/TopicNameDialog.tsx common/NameDialog.tsx`（它的 props 是 title/description/placeholder/confirmLabel/initialValue/onConfirm，与 topic 无关）。杀 `library ⇄ library/topic` 和 `library ⇄ talk`。纯移动 + 一次改名；`tests/ui/components/{file-title,topic-shelf}.test.ts` 的路径要跟着改。
5. `cardRegistry`：`chat/cardRegistry.ts:9-10` import `../info/InfoCards` 和 `../reader/RehearsalCard`，两边都有回边。动作：在 `chat/` 加 `CardRegistryContext`，`chat.tsx:198` 从 context 取表；`cardRegistry.ts` 上提到 `src/ui/components/cardRegistry.ts`，只由 `App.tsx` 和 `InfoCall.tsx` import 并 provide。杀 `chat ⇄ info`、`chat ⇄ reader`。不是纯移动——只搬文件不改 `chat.tsx` 的 import，环还在。`CardKind` 的编译期穷尽检查放在上提后的那张合并表里。
6. `PullToAsk`：`info/InfoHome.tsx:71` import `../phone/PullToAsk`，而 `phone/PhoneHome.tsx` import 回 `../info/{HomeCard,InfoHome}`。InfoHome 是两个外壳共用的屏（`App.tsx:1901`、`PhoneApp.tsx:226`）。动作：把 `pullToAsk?: boolean` 换成 InfoHome 已经在用的那种 render prop —— `wrapScreen?: (opts, children) => ReactNode`，`PhoneApp.tsx` 传 `(o, kids) => <PullToAsk {...o}>{kids}</PullToAsk>`。杀 `info ⇄ phone`，`phone/` 回到 docs/22 说的"只放手机外壳自己的屏"。不是纯移动。

规模：合计 ~400 行改动，绝大部分是 import 路径。
风险：`bunx shadcn add` 之后生成的文件会写回 `@/ui/components/common/...`；`components.json` 的 `ui`/`lib` 别名不要动，只有 base 是新的。
验证：每个子动作后 `bun test tests/layering.test.ts`，`KNOWN_CYCLES` 少一行；全部做完白名单清空并删掉这个机制。`bunx tsc --noEmit`。

### B2 拆 `EmbedPdfView.tsx`

2387 行里约 1690 行不含 React。两块：

- `TouchInputRouter`（428-1166）整个组件 `return null`（:1165），函数体是一个 useEffect 包着 448-1157 的 `attach(el)` 闭包。已核：这个闭包只读外部的 `ctx` 和 `documentId` 两个标识符。动作：`src/reading/engine/attach-touch.ts` 导出 `attachTouchRouter(el, { documentId, ctx }): () => void`，组件缩到 ~15 行的视口轮询 effect。这一块是干净的剪切粘贴。
- `cap`(1425)、`findQuoteRects`(1435)、`wireEngine`(1460-2387)，共 965 行，零 hook 零 JSX，React 只以 `MutableRefObject` 类型出现。动作：`src/reading/engine/wire-engine.ts`。

这一条不能当纯移动做：`wireEngine` 的签名用到 `EmbedPdfViewProps`、`QuoteHighlight`、`AnnotationAnchor`、`PagedGestureCtx`，四个都声明在 `EmbedPdfView.tsx` 里，`PagedGestureCtx`(:343) 还没导出。从新文件 import 回去就是一个类型环，测试会红。必须先把这四个类型单独挪进 `src/reading/engine/types.ts`，再搬函数。

规模：三步提交，末尾 `EmbedPdfView.tsx` 落在 ~650 行。
风险：全项目没有一个测试 import 这个文件（`grep -rl EmbedPdfView tests/` 为空），拆完只能靠真机验。拆分本身不改行为，但 `attach` 里有坑 37/41/45/63/67 的补偿逻辑（含 :1135-1156 拆卸时那句 `el.style.visibility = ""`），漏搬一行就是一个只在 iPad 上复现的回归。
验证：先写测试再拆——`tests/reading/engine/attach-touch.test.ts`，挂到游离的 div 上喂合成 pointer 序列：单指跟随 scrollTop、松手惯性、第二指抬起不留残余滚动、engine 见过的点不重复派发。拆完在 iPad 上跑一遍翻页/长按/选中。

### B3 `reading/engine/gesture/` 子域

前置 A1、B2。`reading/engine` 23 个文件、17 个非测试，超过 15。移进去：`touch-routing.ts`、`paged-gesture.ts`、`vertical-gesture.ts`、`rubber-band.ts`、`scroll-indicator.ts`、`touch-debug.tsx`、新的 `attach-touch.ts` 及各自的测试。

不要跟着移 `layout-settle.ts` 和 `page-frame.ts`：它们的头部注释整段在讲 EmbedPDF 插件的行为（`setScrollStrategy` 内部重算虚拟项、viewport 插件把每次滚动再推迟一帧、坑 42），是引擎知识写成了纯谓词，属于引擎那半。

在 LAYER 登记 `reading/engine/gesture`。性质：纯 `git mv` + 改 import。
验证：`bun test tests/reading/engine/`、`tests/layering.test.ts`。

### B4 平台持久化收口

前置 A2、A3、A10。A2/A3 已经把洞堵上，这里做成一个抽象：

- `src/platform/app/debounced-writer.ts`：`createDebouncedWriter<K>({ write, debounceMs, onError, exit = observeAppExit, timer })`。`annotations.ts:52-79` 与 `threads.ts:143-172` 是同一份 `bindPagehide` + `schedule`（threads 多一个 headless 守卫），`settings.ts:154-165` 是第三份没有 flush 的，`App.tsx:619-644` 是第四份且住在 `.tsx` 里、退出路径吞异常（`.catch(() => {})`）而防抖路径 toast。四份改写到一个上；阅读位置那份搬到 `src/reading/reading-position.ts`。注意 `lifecycle.ts:79` 的注释说的是它不做幂等去重，接进去时要自己保证只 flush 一次。
- `src/platform/app/store-errors.ts`：六个单槽换成 `reportStoreError(scope, e)` + `onStoreError(cb)` 用 `Set` 而非单槽（现在是后注册者静默顶掉前一个），`StoreScope` 是闭合联合并自带用户可见文案。
- `src/platform/sync/pull-routes.ts`：`registerPullRoute({ id, matches, onPulled })`。现在有四个互不相识的订阅者各自重推子集：`App.tsx:566`、`PhoneApp.tsx:168`、`InfoHome.tsx:188`（经 `info/briefing/reader.ts:167-173` 的 `isReaderFile`）、`briefing/live.ts:816`，没有任何东西把它们和 `syncFs.ts:55-84` 的 `ROOT_FILES` 连起来。

规模：~250 行。
风险：`annotations.ts`/`threads.ts` 的 `dropThreadCache`/`dropAnnotationCache` 对外 API 不能变，同步拉取依赖它。
验证：`tests/platform/debounced-writer.test.ts`（假时钟：防抖合并、退出 flush、错误上抛）；`tests/platform/store-errors.test.ts`（每个 scope 有文案；grep 式断言 `store-errors.ts` 之外不再有 `let on…: (e) => void = () => {}`）；`tests/platform/sync/pull-coverage.test.ts`（`ROOT_FILES` 每个字面量和 `inSyncRange` 接受的每个 per-key 模式，要么被某条路由匹配，要么列在显式的 `NO_IN_MEMORY_STATE` 白名单里）。顺手把 `tests/annotations.test.ts` 里三次 600ms 真 sleep 换成假时钟，该文件从 1.87s 降到毫秒级。

### B5 预算阶梯泛化，两个 turn 去重

`reading/turn.ts:523-582` 与 `reading/talks/turn.ts:336-388` 是逐行同构的 ~55 行；此外 `configuredModel`（206 / 132）、`piContext`（212 / 138）逐字相同，`toAnnotationLite`（`turn.ts:590` / `talks/material.ts:38`）只差注释，`HISTORY_KEEP = 40` / `HISTORY_KEEP_TIGHT = 6` 两边各声明一次且 `talks/index.ts` 还把副本再导出一遍。

同时 `budget/ladder.ts:22-31` 的 `ReductionId` 是一串阅读领域的材料名，`:40-76` 的 `LADDER` 连用户可见英文文案一起写死在 capability 里。

动作：`budget/` 保留泛化后的机制——`planReductions`、`budgetNotice`、`REFUSE_FLOOR_OVER`/`REFUSE_EXHAUSTED`，按 `readonly Rung<Id>[]` 和 `Id extends string` 参数化；新增 `budget/fit.ts` 的 `fitToBudget({ model, tools, composePrompt, composeMessages, rungs, purpose })`，两个 compose 以闭包传入，`budget` 仍然不 import 任何 src 目录。具体阶梯和文案移到 `src/reading/ladder.ts`（`READING_LADDER`）和 `src/reading/talks/ladder.ts`。`configuredModel`/`piContext`/`HISTORY_KEEP` 收到 `reading/turn.ts` 一份，`toAnnotationLite` 收到 `src/fulltext/format.ts`（`AnnotationLite` 的声明处，:14）。

规模：~200 行。
风险：坑 65——请求超额时返回只多一个 token、状态正常、无报错。阶梯改错是静默的。
验证：改之前先给两条现有路径各补一个断言 notice 文案和 rung 顺序的测试，改完必须逐字不变。

### B6 章节模型统一，修 deck 规划的空章节

同一条记录有三份定义：`notes/types.ts:37` 的 `NoteChapter`、`rehearsal/types.ts:29` 的 `RehearsalChapter`、`slides/plan.ts:22` 的 `PlanChapter`。同一个投影派生了两次且分叉：`rehearsal/skeleton.ts:21-39` 在没有笔记时回退到 `chaptersFromOutline(fulltext.outline)`，再回退到整本；`slides/live.ts:124-140` 的 `planMaterial` 只读 `loadNotesState(bookId)`，没有就给 `chapters: []`。而 `listDeckTalks`（`live.ts:104-112`）只要任一材料有笔记就放行，`buildPlan`（`live.ts:253`）对每个 `bookId` 无条件调 `planMaterial`——所以一场讲里第二本没笔记的书会拿到空章节表，再被交给 `validateDeckPlan`/`citableWithOutline` 当作可引用集合。

动作：`notes/types.ts` 声明 `BookChapter {index,title,startPage,endPage}`，另两个 extend 它（rehearsal 和 slides 本来就依赖 notes，不新增目录边）；删掉 `planMaterial` 的章节循环，改从 `loadMaterial()` 的 `skeleton.chapters`（`talks/material.ts:49`）取——slides 已经 import 了 talks/store。

规模：~60 行。
验证：新写用例：两本书一场讲、第二本无笔记，`buildPlan` 的可引用章节应与 `buildSkeleton` 一致而非为空。

### B7 把多设备协调规则从 `live.ts` 里拿出来

`live.ts` 这套约定（真实 Tauri/HTTP 依赖绑到注入式纯管线）是好的，`prep/live.ts`(294)、`notes/live.ts`(167)、`subagent/live.ts`(38) 都很薄。两个长坏了，且 `rg 'from ".*live"' tests/` 为空——没有任何测试碰过它们。

- `info/briefing/live.ts` 847 行。605 行处自己挂了一条横幅，之后是十个模块级 `let`（claim、collecting、sessionStartedAt、heartbeat、unsubSync、unsubPulled、syncing、lastSyncAt、electionAt、electionValue），含 60s 选举结果 TTL(:624)、写入即失效(:700)、`took` 分支要触发 `getInfoCollector().refresh()` + `getInfoPipeline().init()`(:711-714)。动作：`src/info/briefing/presence.ts`，`createCollectorSession(deps)` 暴露 `start/stop/publishClaim/amICollecting/onPulled`，依赖 `{ readClaims, writeClaim, readAsks, loadDeviceSettings, siteStates, now, setInterval, clearInterval, pipeline, collector }` 全部注入——注意上半部有七处反向引用，必须做成注入回调而不是文件级反向 import，否则 `presence.ts` 和 `live.ts` 互相 import。
- `observation/live.ts` 660 行，`let sweeping`(:359)、`let guessing`(:463)、`const inFlight`。动作：`src/observation/sweeps.ts`，注入 `{ collectArrears, distill, isThreadBusy, now, schedule }`。

规模：各 ~250 行搬迁。
风险：这两处决定的是"哪台机器花钱采集"和"简报会不会跑两遍"，改错的失败模式最难手工复现。
验证：先写测试再搬——`tests/info/collector-session.test.ts`（假时钟：选举 TTL、take/give-up 转换、ask 去重）、`tests/observation/sweeps.test.ts`（重入）。

### B8 `InfoCall` / `InfoHome` 的控制器下沉

前置 B1.3（`ToolStatus` 移走）。`InfoCall.tsx` 542 行，JSX 只有 500 和 507 两个 return，之前全是编排：线程加载/持久化(165-190)、简报任务订阅与卡片落盘(192-224)、`runBriefingJob`(240-269)、`insertProbeCard`、`handleAddFromCard`(293)、`handleApplyProfile`(344)、`onCardAction` 七分支(375-408)、`runAgent`(413-478)。`InfoHome.tsx` 651 行，JSX 从 513 起，前面 13 个 useState、18 个 useCallback、6 个 useEffect，含站点会话流程(252-306)、来源 CRUD(235-354)、三个 anchor 组装(423-508)。

`talk/useTalk.ts`(438 行，`TalkView.tsx` 只有 169 行) 就是项目自己的先例。

动作：`src/ui/components/info/use-info-call.ts` 和 `use-info-home.ts`。真正与渲染无关的那几段再往下推一层：简报任务的状态机进 `src/info/companion/call.ts`（`trackedJob`/`runnableJob` 旁边），apply-profile 序列（`loadProfile → replaceDeclared → saveProfile → 提议 retriage`）进 `src/info/companion/`，三个 anchor 进 `src/info/companion/anchors.ts`（`InfoCallAnchor` 类型一起从 `InfoCall.tsx:67-82` 搬过去），站点会话时序进 `src/info/sources/session-flow.ts`。

注意这不是规则 2 的违反——规则 2 禁止的是把领域编排塞进 capability，`ui/components` 不是 capability。做它的理由是规则 3 和可测性。

规模：~500 行搬迁，两个文件各落到 ~150 / ~300 行。
验证：新测试断言：已标 `added` 的 probe 卡再确认是 no-op；apply-profile 只写 declared 那半，且只在 `collecting && 已有简报` 时才把 `canRetriage` 置真；请求撞上正在跑的任务时并入同一张卡而不是新开一行；失败的任务其 note 不落盘；`keepArticle` 在 `state.kind !== "body"` 时拒写；article anchor 按 mustRead → oneLiners → outOfLane 取理由并回退到 null。

### B9 两个手机手势 hook 的 DOM 层合一

纯机器（`pull-to-ask-gesture.ts`、`edge-back-gesture.ts`）该不同，DOM 那半是抄的：`easeOut` 逐字相同（`usePullToAsk.ts:41` / `useEdgeBack.ts:44`）；rAF 动画闭包（123-147 / 93-116）逐行相同，只差调哪个 paint 和 `useEdgeBack` 多一个 `done?.()`，连 `stopAnimRef` 自清那段都一样；`touchRef` 的类型和三行注释相同；touchMove 体相同（含 `Array.prototype.find.call` 的 cast）；八对监听器的安装/拆卸块相同，连两条注释都一样。

动作：`src/ui/components/phone/gesture-dom.ts`，导出 `easeOut`、`createAnimator(paint)`、`bindGesture(host, { pointerTarget, onPointer, claim, enabled })`。
规模：~150 行，两个 hook 各减 ~60 行。
风险：这里编码的是坑 37/38/41/70 的双通道 pointer/touch 规则。
验证：新写 `tests/ui/components/gesture-dom.test.ts`；iPad 上手工验边缘返回和下拉提问各一次。

### B10 `covers.ts` 不再直接开 PDFium

`reading/covers.ts:27` 直接 import `@embedpdf/models` 的 `PdfDocumentObject/PdfEngine/PdfRenderPageOptions`，自己跑 `openDocumentBuffer → renderPage → closeDocument`，还带着坑 102 的双 quality key 补丁。项目学到的三个引擎怪癖里有两个现在记在两个目录。`rg -l '@embedpdf' src` 只有六个文件，五个在 `reading/engine`，第六个是它。

动作：`src/reading/engine/raster.ts` 导出 `renderFirstPageJpeg(bytes, { scaleFactor, quality })`，含开/渲/关序列、两个 20s 超时和 quality 怪癖，返回判别联合（`ok | timeout | open-failed | no-pages | render-failed`）；缓存与失败标记策略留在 `covers.ts`，它从此看不见 `PdfEngine`。
规模：~80 行。
验证：加一条契约断言 `src/reading/engine/` 之外无文件 import `@embedpdf`（`smoke/` 例外）。

### B11 `MicButton` 的按压状态机

254 行，五个可变量（`busyRef`/`releasedRef`/`startedRef`/`cancelArmedRef`/`status`，:54-57）跨 `begin`(97)、`finish`(131)、`onPointerDown`(160)、up/leave/enter(175-190)、window 级 pointerup+Escape effect(76-95)、卸载取消(69-75) 互相改。四条竞态规则只写在注释里：arming 期间松手要让 `begin()` 取消它刚起的录音；缺 STT key 的上报排在"提前松手"的 bail 之前；leave 只在真的在录时才 arm cancel；卸载要取消在录的一次。`tests/ai/voice/` 下没有它的测试，而 `composer.test.tsx:14` 已经有从 `chat.tsx` 里 import 纯函数来测的先例。

动作：`src/ai/voice/press-machine.ts`，`pressReducer(state, event)` 覆盖 `down|up|leave|enter|escape|started|failed|transcribed|unmount`，产出 `{ status, cancelArmed, effects }`。组件留 pointer 绑定、计时和 JSX。
规模：~120 行。
验证：`tests/ai/voice/press-machine.test.ts`：down 后在 `started` 到达前 up → 出 cancel 且永不 transcribe；leave 再 enter 后松手仍 transcribe；录制中 Escape 取消且不 insert；录制中卸载恰好发一次 cancel。

### B12 info 侧的 HTTP 补 Retry-After

`info/extract/http.ts` 的 `fetchText` 直接 `continue` 重试，不看 `Retry-After`。

info 侧有节流，只是换了机制：`info/sources/pool.ts` 是跨源的并发闸，每个源的间隔由描述符的 `pollMinutes` 定（`builtins.ts:192-194` 写明 Nature 的 feed 因此设 1440）。`reading/papers/http.ts` 的按主机间隔表针对的是另一件事——arXiv 和 Semantic Scholar 对交互式查询有明文速率政策。两套机制各自成立，不合并，也不新增 `src/net/` capability。

动作：`fetchText` 补 `Retry-After` 解析和退避 sleep。`isTauri` 和 `FetchFn` 的统一在 A9 已经做掉。
规模：~10 行。
验证：假时钟断言 429 带 `Retry-After` 时的等待和退避上限。

### B 的顺序与并行

必须先做：A1 → B1；B2 → B3；A2/A3/A10 → B4；B1.3 → B8。

可以并行开在各自 worktree（互不碰同一文件）：

- 通道一：B1（六个子动作里 1→2→3 串行，4/5/6 可再分三个 agent）
- 通道二：B2 → B3
- 通道三：B4 → B12
- 通道四：B5、B6（都在 reading 的算料侧，串行更省事）
- 通道五：B7（两个文件互不相干，可再分两个 agent）
- 通道六：B9、B10、B11（各自独立小活）
- B8 排在 B1 之后，且与 B1 通道共用 `src/ui/components/info/`，不要同时开。

`src/App.tsx` 是唯一的高冲突点：A2、A3、B1.3、B1.5、B4 都会碰它。这五项按 A2 → A3 → B1.3 → B1.5 → B4 严格串行落地，别并行。

---

## C 盯着，还不到时候

- 把三份流式对话驱动抽成 `ai/turn-stream.ts`。`App.tsx:779-949`、`useTalk.ts:214-344`、`InfoCall.tsx:413-478` 的共同残余其实只有 ~8 行，且已经走 `toolTrace` 的共享函数；三者的失败分类、拒答处理、partial 保留策略是三套不同的设计而不是三份抄写。触发线：出现第四个流式对话宿主，或者三者之间再出一个像 A4 那样的行为分叉 bug。
- 把 `App.tsx` 的阅读会话（`runTurn`、`openInReader`、`closeReader`、19 处 `setCall`）整体抽成 `reading/session/` 的 hook。原来的理由是"手机形态要复用"，但 docs/22 写明手机上永远不开 PDF、不划线、不看笔记，是永久决定，而 iPad 用的就是 `App.tsx`——没有第二个宿主。触发线：`App.tsx` 过 2500 行，或者 `App.tsx` 之外的外壳真的要装阅读器。可以先只做便宜的那一半：`CallState` 的转换规则现在是 19 处 spread 的涌现结果（`chat-pip` 两处守卫已经不同），把它做成 `src/reading/call-state.ts` 的纯 reducer + `useReducer` 是值得的，等 `App.tsx` 下次被大改时顺手做。
- A10 加了 `readJson` 之后，38 处调用点的改写。新写的 store 一律用新函数；旧的等到读失败策略真的要改（隔离、告知用户、加 schema 版本闸）的那天一次性改完。
- 未声明的色阶 token 化：`#777`×31、`#999`×26、`#eee`/`#ececec`/`#e6e6e6`×16、`#b45309`×5、`#5fb236`×5。触发线：做深色模式或换配色。
- `loadPdfjs`（`fulltext/extract.ts:154`）提成独立 capability。`extract.ts:150-153` 已经说明共用是刻意的（避免第二个 worker 和第二个 chunk）。真正的债是 `render.ts:57-72` 的 `Promise<any>` 和 `store.ts:76` 的 OPS cast。触发线：pdf.js 大版本升级，或出现第三个消费者。
- `tests/` 不再镜像 `src/`：19 个文件直接躺在 `tests/` 根（16 个测 `platform/app`），`tests/info` 28 个平铺、`tests/ui/components` 27 个平铺。规则 1 的措辞只管 `src/`，所以这不是违规。触发线：任一目录过 40 个文件。
- `ui/components/{library:14, reader:13, settings:12}` 逼近 15。B1 之后 library 会降到 6。另两个到 15 再说。

---

## D 明确不做

- `ui/components/ui`（16 个文件）按规则 1 拆分。路径由 `components.json` 的 `ui` 别名钉死，`bunx shadcn add` 按它写文件。同理 `ui/components/lib` 只有一个 9 行的 `cn`，也由别名钉死。
- 把 `SourcesPage.tsx:233-243` 的裸 `<input>`、`AnnotationPopup.tsx:131-132` 的裸 `<textarea>` 换成 `Input`/`Textarea` 原语。项目自己的文档解释过这两处为什么是裸的。A6 只收下拉行那部分。
- 把 `reading/sources/article.ts` 的 `extractArticle` 换成 info 侧的 `extractReadable`。两者契约不同：前者返回纯文本 + 标题 + `ARTICLE_MAX_CHARS` 截断，喂 Fulltext 页和摘要 prompt；后者返回 `{title, contentHtml, textContent}`、不截断、且在没有 DOMParser 的环境返回 null。A9 只合并 `decodeEntities`/`htmlToText` 两个纯函数。
- 拆 `info/briefing/pipeline.ts`（901 行）。一个类一件事，六个阶段已经分别委托给 `screen.ts`/`triage.ts`/`item-pool.ts`/`run-state.ts`/`store.ts`，剩下的是编排加约 150 行的 observable/checkpoint 脚手架。最近的接缝会把一个状态机劈成两半。
- 把 `reading/figures → fulltext` 当成分层违规修掉。domain → capability 是 `MAY_IMPORT` 明确允许的方向。
- 把 `InfoHome` 的 `renderLaunch`/`onOverlayChange`/`pullToAsk`/`pipCards` 四个 prop 当成规则 4 违规。规则 4 管的是叶子组件不按形态分叉，render prop 洞正是 docs/22 定下的机制。B1.6 只修 `pullToAsk` 那一处 import 方向。
- 把 `budget/ladder.ts` 当成"领域编排塞进 capability"来判。规则 2 那句针对的是编排代码，`ladder.ts` 是一张数据表加一个纯函数。B5 做它的理由是两份 turn 的重复，不是分层。
- 把 `reading/saved-articles.ts` 搬去 `info/` 当成规则 2 违规。domain → domain 合法且无环。A9 之外 A 里那条移动是整理偏好，不是修违规——不做也行，但它是全项目唯一一条 `reading → info` 的边，顺手清掉更干净。
- `platform/sync ⇄ platform/sync/merge` 不是环，只有单向边。

---

## 规则要改的地方

- `tests/layering.test.ts` 头部第 23-27 行那条设计声明（"解析停在两段，更深一层的目录折进上面那个单元，不需要 key"）已经被穿透，改成"按最长 LAYER key 前缀解析，任何含源文件的目录都必须登记"。A1 就是这条。
- 规则 1 的"约 15 个文件"加一条例外：由生成器/别名钉死路径的目录（`ui/components/ui`、`ui/components/lib`）不适用。
- 规则 3 的理由要改，待定，项目发起人还没定。现在写的是"手机形态将来换掉的是 `.tsx` 那一层"，但 docs/22 已经定死手机上永远不做书，iPad 用的是 `App.tsx`——阅读那一侧的 `.tsx` 没有第二个消费者，这个理由推不出该做的工作量。候选写法：不依赖 React 的逻辑放 `.ts` 是为了能单测，形态可移植是附带收益。这条怎么定，决定了 `App.tsx` 的拆分和 B8 的 `InfoCall`/`InfoHome` 控制器抽取是排在 B 还是留在 C。
- 规则 1 明确写上它只管 `src/`（或者反过来，把 `tests/` 也纳入并接受要做一次搬家）。现状是既不管也没做，读起来像欠账。

## 要加的契约测试

- `tests/layering.test.ts`：A1 的深度解析 + 强制登记；`KNOWN_CYCLES` 白名单在 B1 结束时清零并删除该机制。另加一条显式规则"`budget` 不 import `ai`"——今天它成立只是因为 `ai → budget` 存在（`ai/agent.ts:39`、`ai/model-call.ts:7`）会触发环检查，那条边一旦消失，`budget → ai` 就变合法了。
- `tests/platform/sync/pull-coverage.test.ts`：`ROOT_FILES` 的每个字面量和 `inSyncRange` 接受的每个 per-key 模式，必须要么被一条已注册的 pull route 匹配，要么列在测试里的 `NO_IN_MEMORY_STATE` 白名单——形状照 LAYER 表，新增同步文件在有人分类之前一律构建失败。
- `tests/platform/store-errors.test.ts`：`StoreScope` 每个成员有文案；`store-errors.ts` 之外不再有 `let on…: (e) => void = () => {}` 形式的单槽；两个外壳都注册。
- `tests/ui/components/primitive-contract.test.ts`：补菜单行 44px 断言。
- 新增一条断言：`src/ui/components/` 下不出现与已声明 token 取值相同的 `[#...]` 字面量（A7 之后才能加，先只管那八个值）。
- 新增一条断言：`src/reading/engine/` 与 `smoke/` 之外无文件 import `@embedpdf`（B10 之后）。

---

## 已核实健康，不要重审

导入图是从源码机械重建的，不是照 LAYER 表读的：真实目录粒度 738 条边 / 188 个目录对，`@/` 已解析，零个未解析 specifier，src 下没有 `require()`、没有非字面量的动态 import。

- capability 纯净性事实成立：`ai`、`ai/subagent`、`ai/voice`、`budget`、`fulltext` 里没有任何文件 import `reading`、`info`、`observation`、`ui`、`App.tsx` 或 `PhoneApp.tsx`。`budget` 在 src 下什么都不 import。
- `platform/app` 零条出边。
- 除 `main.tsx` 外没有任何文件 import `App.tsx`/`PhoneApp.tsx`。
- 领域和子领域图在真实目录粒度下无环。逐对追过并排除：`reading/notes → reading/figures` 无反向边；`reading/slides → reading/{figures,notes,talks}`；`reading/talks → reading/{figures,notes,rehearsal,reading 根}` 且无边回 slides；`info/briefing → info/{sources,extract}` + `observation`；`info/sources → info/extract` + `observation`；`info/companion` 指向所有人，无人指回。
- `platform/sync → platform/sync/merge` 单向，`merge/` 是干净的叶子。
- `ai/` 和 `fulltext/` 查过有没有夹带领域编排，没有：`fulltext/format.ts` 的 `TopicMaterial`/`AnnotationLite` 是三个领域共用的 prompt 渲染形状，headless；`budget/estimate.ts` 的 `BudgetPurpose` 是尺寸档位不是领域词汇。只有 `budget/ladder.ts` 带领域内容，那是 B5 的事，不是分层违规。
- `ui/components` 直接 import 领域，是项目自己的 MAY_IMPORT 表允许的方向。
