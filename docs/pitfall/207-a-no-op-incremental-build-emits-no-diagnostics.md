# 207 增量构建零变化时，`xcodebuild` 一条诊断都不输出

## 现象

同一个 commit 连续跑两次编译检查，第二次零 warning、零 error。

## 原因

没有文件变化就没有重新编译，也就没有新的诊断产生——诊断是编译这一步的副产物，跳过编译就跳过诊断，不代表警告已经消失。

## 解法

第二次跑出来的"零 warning"不能当成"警告已修"的证据。要看到全部诊断，得先强制重新编译（`scripts/ios-swiftcheck.sh --clean` 会先删掉 derived data 再编）。
