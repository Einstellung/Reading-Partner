// Stub. See ../unsupported.js. Only foliate's MOBI path imports this; the app's
// own unzip is the fflate package.
import { unsupported } from '../unsupported.js'
export const unzlibSync = () => unsupported('foliate fflate bundle')
