# 依赖
node_modules/

# 构建产物
dist/
build/
src-tauri/target/
src-tauri/gen/

# 第三方源码的本地副本，只用于阅读分析，不入库。
# vendor/reader = github.com/zotero/reader（AGPL-3.0），见 docs/00-项目共识.md 第五节。
# 将来正式嵌入时会把它转成 git submodule（路径不变），届时解除这条 ignore。
/vendor/

# 密钥：BYOK 模式，API key 只存本地，绝不入库
.env
.env.*
!.env.example

# 编辑器 / 系统
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json
*.swp

# 日志
*.log
npm-debug.log*
