// App code only. Test files (here and under tests/) are checked by
// tsconfig.test.json, which adds the bun runtime types; `types: []` keeps those
// globals out of here so a `Bun.` call can never compile into webview code.
{
  "compilerOptions": {
    "target": "ES2020",
    "useDefineForClassFields": true,
    "lib": ["ES2020", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "skipLibCheck": true,
    "moduleResolution": "bundler",
    "allowImportingTsExtensions": true,
    "resolveJsonModule": true,
    "isolatedModules": true,
    "noEmit": true,
    "jsx": "react-jsx",
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noFallthroughCasesInSwitch": true,
    "types": [],
    // `@/` is what the shadcn CLI writes into a generated component. It is
    // reserved for that: app code imports relatively, so tests/layering.test.ts
    // sees every cross-directory edge as a path it can resolve.
    "baseUrl": ".",
    "paths": { "@/*": ["./src/*"] }
  },
  "include": ["src"],
  "exclude": ["src/**/*.test.ts", "src/**/*.test.tsx"],
  "references": [{ "path": "./tsconfig.node.json" }]
}
