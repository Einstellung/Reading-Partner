// One assembly for every turn (docs/61): a laid desk plus what is known about
// the reader, priced against the model's window.

export { assembleTurn, configuredModel, type AssembleInput, type AssembledTurn } from "./turn";
export { brainMemorySection, openBrain, type Brain } from "./memory";
